describe("Testing our nodeJS app", () => {
  it("Testing using Github Actions", () => {
    expect(2).toBe(2);
  });
});

describe("testing #2 our nodeJS app", () => {
  it("Testing using Github Actions", () => {
    expect(3).toBe(2);
  });
});
